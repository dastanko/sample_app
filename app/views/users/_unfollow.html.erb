<%= form_for(current_user.relationships.find_by_followed_id(@user.id), :remote => true, :html => {:method => :delete }) do |f| %>
    <div class="actions">
      <%= f.submit "Unfollow" %>
    </div>
<% end %>